template: Hello, %s!
defaultName: Stranger

server:
  applicationConnectors:
    - type: http
      port: 80
  requestLog:
      appenders: []
logging:
  appenders:
    - type: console
      threshold: WARN